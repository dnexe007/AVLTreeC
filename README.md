# AVL дерево
Упорядоченное дерево для хранения и поиска элементов с уникальными строковыми ключами и вещественными значениями. Ключ – непустая последовательность латинских букв не более чем из 6 символов. Ключи упорядочиваются лексикографически. Дерево требует, чтобы разница высот была не больше 1, в противном случае совершает балансировку с помощью вращений.

## Требования
* ОС Windows 10/11
* Visual Studio 2022 c инструментами разработки на C++
* При невозможности выполнить требования необходимо адаптировать `makefile` под используемую ОС и компилятор

## Запуск
1. Открыть проект в Visual Studio
1. В Visual studio открыть developer prompt (комбинация CTRL + \`)
2. Переместиться в подпапку - `cd AVLTreeC`
3. Запуск программы - `nmake run`
4. запуск с чтением команд из файла - `nmake run ARGS="commands.txt"`
5. запуск с чтением команд из файла и записью вывода в другой файл - `nmake run ARGS="commands.txt output.txt"`
6. Запуск тестов - `nmake test`

## Поддерживаемые операции
1. добавление элемента: `add [key] [float number]`
2. удаление элемента: `remove [key]`
3. печать дерева `print`
4. поиск по дереву: `search [key]`
5. выход из программы `exit`

## Ход решения
Для удобства API дерево разделено на 2 структуры - `Tree` и `Node`. `Tree` в себе хранит указатель на корневой `Node`. Взаимодействие с деревом производится через методы для `Tree`. Эти методы вызывают соответствующие методы из `Node`. Сами алгоритмы работают через методы Node, часть из которых инкапсулирована. Например, для балансировки используются методы `RotateLeft()` и `RotateRight()`. Снаружи вызвать их невозможно, они доступны только в пределах файла `node.c`.
Данные узла хранятся в виде структуры `NodeData` (char* и float)
Методы добавления, удаления и вывода дерева реализованы рекурсивно. Метод поиска - итеративно.
Есть три варианта работы с программой: 
1. ввод вручную в консоль, вывод в кончоль
2. ввод из файла, а вывод в консоль
3. ввод из файла, вывод в другой файл


## Вывод
В ходе работы был изучен принцип работы AVL дерева, его алгоритмы балансировки, вращения. AVL дерево позволяет повысить эффективность поиска засчет балансировки - сложность в худшем случае O(log n) вместо O(n).
Также была изучена возможность языка C передавать аргументы в программу.
